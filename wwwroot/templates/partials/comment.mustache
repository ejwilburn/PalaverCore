                {{#IsUnread}}
                <div class="panel panel-default unread" data-id="{{Id}}">
                {{/IsUnread}}
                {{^IsUnread}}
                <div class="panel panel-default" data-id="{{Id}}">
                {{/IsUnread}}
                    <div class="panel-heading">
                        <h3 class="panel-title">{{UserName}}<small class="pull-right">[{{CreatedDisplay}}]</small></h3>
                    </div>
                    <div class="panel-body">
                        {{Text}}
                    </div>
                    {{#Comments}}
                        {{>comment}}
                    {{/Comments}}
                    <div class="panel-footer btn-group btn-group-xs">
                        <button type="button" class="btn btn-default" onclick="newReply({{ParentCommentId}})"><span class="glyphicon glyphicon-console" /></button>
                        <button type="button" class="btn btn-default" onclick="newReply({{Id}})"><span class="glyphicon glyphicon-share-alt flip" /></button>
                    </div>
                </div> <!-- Comment {{Id}} -->
